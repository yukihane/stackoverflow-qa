/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package jaso92559.app;

import jaso92559.list.LinkedList;

import static jaso92559.utilities.StringUtils.join;
import static jaso92559.utilities.StringUtils.split;
import static jaso92559.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
